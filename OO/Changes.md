# 测试库基准线变更记录

记录以 `tbcmangosdev`（测试库）为基准建立新基准线过程中，从各数据源同步过去的数据变更，以及后续 bug 修复、upstream 合并记录。跟 `fix.sql`（单条内容/bug修复）分开记录。

## 基准线同步（2026-08-04，已完成）

- **locale 全量同步**：`tbcmangosdev` 从旧生产库 `tbcmangos` 同步 12 张 `locales_*` 表（`mysqldump --no-create-info --replace`，按主键 REPLACE INTO），全部通过 `CHECKSUM TABLE` 核对一致。`tbcmangosdev` 独有的 6 张新版本表（`locales_area`/`locales_areatrigger`/`locales_broadcast_text`/`locales_faction`/`locales_spell`/`locales_taxi_node`）未受影响。✅
- **gameobject 中文名不显示**：根因是官方迁移 `s2485_01_mangos_closing_text.sql` 在 `tbcmangosdev` 上只做了一半——`gameobject_template` 已改，但 `locales_gameobject` 的 `castbarcaption_loc1..8` 没跟着改名成 `opening_text_loc1..8`（代码本身没问题，一度误判去改代码，已改回原样）。已在 `tbcmangosdev` 补跑迁移，验证 173216/180511 的 `name_loc4` 中文数据能正确读取。`mangos-tbc`(v2)/`tbcmangos2`/旧库 `tbcmangos` 仍有同样缺陷，本次未处理。✅

## Bug 修复记录

### SpellCastArgs 未初始化成员崩溃（2026-08-04）
生产服崩溃日志定位到 Mana Tombs Pandemonius "Void Blast" 连锁施法：`Spell.h` 的 `SpellCastArgs()` 构造函数漏初始化 `m_itemSet`/`m_itemTarget`，栈垃圾值被误判为"已设置物品目标"，下游解引用野指针崩溃。非 Pandemonius 专属，是通用类缺陷，`mangos-tbc` 同样有此问题，本次只修 `Nmangos-tbc`。
修复：`Spell.h` 构造函数初始化列表补 `m_itemSet(false), m_itemTarget(nullptr)`。
**状态**：✅ 代码已改，需重新编译部署。

### 合并 origin/master 两个提交到 v3（2026-08-05）
`6217a6039`（修复交换队伍连发 `SMSG_GROUP_LIST` 导致客户端崩溃）+ `9c1c4121a`（Gnomeregan boss_thermaplugg 空指针判断 + Karazhan"主人的露台门"未跟随舞台事件解锁）。两者都与本地此前独立所做的修复内容重合/等价，`git merge origin/master --no-ff` 合并，`Group.h/.cpp`+`GroupHandler.cpp` 冲突手动解决（保留本地更规范的默认参数写法，功能等价）。
**状态**：✅ 已合并到本地 `v3`（合并提交 `88cbc5c86`），**尚未 push 到 `roowow/v3`**；Karazhan 门修复尚未编译部署。

### 单个技能伤害修复记录（简表，2026-08-07 ~ 2026-08-14）

> 以下是历次玩家反馈的单个/单批 NPC 技能伤害过高修复，统一都是 `SPELL_ATTR_SCALES_WITH_CREATURE_LEVEL` 缩放溢出根因（详见下方"系统性发现"节），逐条排查、按参考数据或经验估算给 `urand`/`irand` 值，均为纯代码 `case`/`Object.cpp` 覆盖，不改动法术ID/数据库。**2026-08-14 起不再逐条写详细叙事，只记一行**；之前几十条的详细分析过程已从本文档移除（root cause 分析方法本身已经充分记录在下方"系统性发现"节，个案的推理过程不再需要保留）。下表所有条目**已于 2026-08-14 全部改用公式自动计算**（见"实施记录"及后续几节），此处的值仅作历史存档：

| 日期 | 技能(ID) | 怪物(entry) | 最终值 |
|---|---|---|---|
| 08-07 | 暗影新星(33501) | 恶毒导师(18848)，暗影迷宫 | 普通1000-1500 / 英雄2000-2500 |
| 08-12 | 冰霜震击(22582普通/37865英雄) | 盘牙先知(17803)，蒸汽地窖 | 普通1700-2100 / 英雄2900-3500 |
| 08-12 | 寒冰箭(12675普通/37930英雄) | 盘牙巫师(17722) | 普通1290-1760 / 英雄2580-3520 |
| 08-12 | 毒液箭(37272普通/37862英雄)+DoT | 泥沼主宰(21694) | 普通1000-1675 / 英雄2000-3350，DoT普通260-436/英雄520-871 |
| 08-12 | 闪电箭(15234普通/37664英雄) | 盘牙海妖(17801) | 普通1200-1750 / 英雄2400-3500 |
| 08-12 | 烈焰风暴(16102) | 伊利达雷大领主(19797)，火红岗哨之战 | 1500-2200 |
| 08-12 | 水箭(32011) | 库斯卡海妖(19768) | 400-700 |
| 08-12 | 暗影箭(20298) | 斯克提斯唤魂者(21911) | 500-800 |
| 08-13 | 奥术箭(13901) | 斯克提斯风行者(21649) | 500-800 |
| 08-13 | 暗影箭(9613，多entry共用) | 暗影议会术士(21302)/死亡熔炉召唤者(20872)/黑暗教团暗影法师(19826) | 350-600 |
| 08-13 | 邪能火球(36247) | 愤怒的火灵(21061) | 400-700 |
| 08-13 | 火焰投石(38498) | 愤怒的地灵(21050) | 400-700 |
| 08-13 | 上古之火(37988，容器法术37986触发) | 日蚀法师(19796) | 400-700 |
| 08-13 | 邪能火球(37945) | 大型魔火双帆龙(21462) | 400-700 |
| 08-13 | 闪电链(12058) | 愤怒的气灵(21060) | 400-700 |
| 08-13 | 能量涌动(36534，容器法术36508触发) | 巴什伊尔奥术师(22243) | 400-700 |
| 08-13 | 火球术(20823) | 日怒大法师(20135) | 400-700 |
| 08-13 | Arcane Bolt(15254) | 虚灵航标(18431)，H法力陵墓 | 1675-2225 |
| 08-13 | 暗影箭(12739)/烈焰冲击(14145)/冰霜新星(15063)/射击(16100，均英雄) | 盘牙麻疯者(21338)，H蒸汽地窖 | 1150-1540 / 1990-2200 / 500-570 / 1280-1650 |
| 08-13 | 剧毒软泥(40818，每跳) | 德拉诺黑血(23286) | 150-300 |
| 08-12 | 火焰之雨(38741，每跳) | 玛卡扎顿(21501)，军团要塞 | 300-600 |
| 08-13 | 寒冰箭(12675，复用)+冰霜新星(15531)+闪电箭(15234/37664，补entry判断) | 荒土奴隶(19902)，H奴隶围栏 | 寒冰箭同盘牙巫师；冰霜新星1040-1180 |
| 08-14 | 烈焰之雨(36808，每跳)/灼烧(36807)/暗影箭(15232)/火球术(17290) | 血环暗法师(18049)/血环占卜者(18050)/地狱火小鬼(18606)，H地狱火城墙+H鲜血熔炉 | 1300-1600 / 1600-2150 / 1750-2350 / 2250-3050 |
| 08-14 | 瞄准射击(38861，武器伤害加成) | 龙喉碎天者(22274/23440/23441/23188) | 1500-2000（**目前唯一仍保留手动覆盖，机制特殊**） |
| 08-14 | 腐蚀术(37113，每跳)/暗影箭(15472) | 影月术士(18619)，H鲜血熔炉 | 300-500 / 1500-2200 |
| 08-14 | 烈焰风暴(16102)/火球术(17290，追加分支) | 影月召唤者(18617) | 复用已有值 |
| 08-14 | 火球术(14034) | 地狱火劣魔(21646)，H鲜血熔炉 | 850-1150 |
| 08-14 | 奥术闪耀(37856，直接命中+每跳) | 盘牙海妖英雄版(20623)，H蒸汽地窖 | 1321-1527 / 600-680 |
| 08-14 | 闪电箭(20295) | 呼啸的裂风者(22310)，纳格兰 | 400-700 |
| 08-14 | 水箭(34425) | 凶猛的水浪之魂(22309)，纳格兰 | 400-700 |

**唯二仍保留手动覆盖的**：`38861`（瞄准射击，机制特殊，NORMALIZED_WEAPON_DMG叠加武器伤害）、`38741`（Rain of Fire，开放世界无数据支撑）。其余全部改用公式自动计算。

### ⚠️ 系统性发现：`SPELL_ATTR_SCALES_WITH_CREATURE_LEVEL` 缩放溢出是批量问题，不止个案（2026-08-12）
排查冰霜震击时用 SQL 批量筛查"带 `SPELL_ATTR_SCALES_WITH_CREATURE_LEVEL` 属性 + `spellLevel` 远低于实际施法怪物等级（差值≥30级）"这个特征，一次性查出 **204个不同法术、涉及247个不同怪物**存在同一种缩放溢出隐患，覆盖奥金顿、蒸汽地窖、赛斯克大厅、卡拉波神殿、影月谷野外怪、旧希尔斯布莱德等大量副本和野外区域（已确认的"暗影新星""冰霜震击"都在这份清单里，验证了排查逻辑正确）。
**结论**：不适合继续用"发现一个、手动查资料、加一个`case`"这种一对一方式处理，204个技能逐个查证不现实。

**排查范围有缺口，实际受影响数量可能更多**：本轮后续修复"烈焰风暴"（16102）、"水箭"（32011）时发现，这两个都是通过老式 `creature_ai_scripts` 系统施法（`creature_template.SpellList=0`），而当初排查204个用的 SQL 只 JOIN 了 `creature_spell_list`（新版系统），完全没覆盖走 `creature_ai_scripts` 的这批怪——204这个数字只是"新系统"部分，"老系统"部分还没排查过，实际总数会更多。以后系统性修复时需要把这条路径也覆盖进筛查 SQL。

**另一处缺口——判定逻辑不是只看"效果类型"，还要看"光环类型"，且不是所有受影响效果都走同一个下游函数**：排查"毒液箭"时发现，缩放判定优先看 `EffectApplyAuraName`（光环类型是 `PERIODIC_DAMAGE`/`PERIODIC_LEECH`/`SCHOOL_ABSORB`/`POWER_BURN_MANA`/`PERIODIC_TRIGGER_SPELL_WITH_VALUE`/`PERIODIC_MANA_LEECH` 这几种照样会被缩放），只有查不到光环类型时才退回看"效果类型"（`SCHOOL_DAMAGE`等）。之前几次修复只处理了"直接命中"这一种（走 `Spell::EffectSchoolDMG`），漏了"持续伤害光环"这一种（`Effect=27`纯光环技能如"火焰之雨"完全不经过 `EffectSchoolDMG`，必须在 `CalculateSpellEffectValue` 本身覆盖）——204个清单里可能还有别的技能是这种"纯光环"或"直接命中+被忽略的DoT"结构，逐个核对时要把 `EffectApplyAuraName1/2/3` 也纳入检查，不能只看 `Effect1/2/3` 的表面类型。

**第三处缺口——法术ID可能只是"容器"，真正的伤害来自它触发的另一个法术**：排查"日蚀法师"上古之火时发现，玩家反馈的法术ID（37986）本身是 `PERIODIC_TRIGGER_SPELL`（光环类型23，纯粹"每N秒触发一次另一个法术"，不在缩放判定名单里，自己不受影响），真正带 `SPELL_ATTR_SCALES_WITH_CREATURE_LEVEL`、造成伤害的是它 `EffectTriggerSpell1` 指向的另一个独立法术ID（37988）。以后核对204清单/处理新反馈时，如果法术本身查出来"没有缩放属性"或"效果类型对不上"，要顺着 `EffectTriggerSpell1/2/3` 再查一层，不能看到"没问题"就直接放过。

**根因定位到公式本身**：`Object.cpp` 的 `CalculateSpellEffectValue`，`value = value * (CLSPowerCreature / CLSPowerSpell)`，`CLSPower*` 来自 `creature_template_classlevelstats.BaseDamage`——这张表按等级增长是陡峭曲线而非线性（同职业20级→70级差21.6倍），公式本身没有任何"等级差过大就封顶/失效"的保护，导致被套用到"20级模板 vs 70级施法者"这种远超设计初衷的极端场景时直接崩坏。**已核实这段代码（含 `// TODO: Drastically beter than before, but still needs some additional aura scaling research` 这条注释）在 upstream mangos-tbc 里逐字节一致存在**——不是 Nmangos-tbc 自己的问题，是继承自官方仓库的共享缺陷，官方自己也承认这块"还需要进一步研究"，不是我们独有的坑。

**候选修复方向**（都是引擎级改动，需要专门离线核算校准后再动手，不适合顺手改）：
- **方案A**：给缩放倍数本身设上限（`std::min(ratio, 上限)`）——简单，但"上限该定多少"缺乏依据，容易一刀切过头或不够。
- **方案B（倾向）**：不直接限制倍数，而是限制查表用的"等级差"（比如 `effectiveSpellLevel = max(spellProto->spellLevel, casterLevel - 20)`），让缩放曲线本身不被拉到离谱区间，同时数值来源仍然是同一套曲线，不是拍脑袋的魔法数字。
- 改动前必须用"暗影新星""冰霜震击"这两个已经手动验证过的正确答案做校准（改完公式后重新算一遍这两个技能，看结果跟目前手动 `case` 覆盖的数值是否接近），改动后要评估要不要撤掉这两个手动 `case` 覆盖（避免两套机制叠加）。

**状态**：⏸️ 待办，尚未开始处理，本次只处理了清单里已被玩家反馈的"暗影新星""冰霜震击"两个，其余202个 + 公式级修复作为独立大任务留待后续排期。排查用的 SQL：
```sql
SELECT DISTINCT s.Id, s.SpellName, s.spellLevel, ct.entry, ct.name, ct.MinLevel, ct.Rank,
       (CAST(ct.MinLevel AS SIGNED) - CAST(s.spellLevel AS SIGNED)) AS levelGap
FROM spell_template s
JOIN creature_spell_list csl ON csl.SpellId = s.Id
JOIN creature_template ct ON ct.SpellList = csl.Id
WHERE (s.Attributes & 0x80000) != 0
  AND s.spellLevel > 0
  AND (CAST(ct.MinLevel AS SIGNED) - CAST(s.spellLevel AS SIGNED)) >= 30
ORDER BY levelGap DESC;
```

### 后续进展：发现 origin/master 已有官方的公式级修复（2026-08-13）

**背景**：核对 `v3` 分支相对 `origin`（Nightingale9002/Nmangos-tbc）远程的 `master` 分支（注意不是 `master3`，两者是不同分支）差异，发现 `v3` 已经**真分叉**——领先50个提交（我们自己的cherry-pick+这次会话的一堆技能修复），落后87个提交。逐条审查尚未开始，但其中一条直接命中了上面这个系统性问题：

**`7c72bb407` "修复法系怪伤害异常"（Nightingale9002，2026-08-13）**：改的正是 `CalculateSpellEffectValue` 里那两处 `GetCreatureClassLvlStats` 调用，把查表用的 `Expansion` 参数从怪物自己的实际版本（TBC=1）**无条件改成 0（原版旧世界）**，作者注释："Use vanilla (Expansion 0) CLS values to avoid overscaling spell damage after the s2488 CLS rework"——即某次代号 s2488 的CLS表重做，把TBC档位（Exp1）的曲线数据搞得过陡，这正是我们这批bug背后更深一层的根因（不是公式逻辑错，是数据源本身在某次改动后跟不上）。

**核实数据**（`creature_template_classlevelstats`，UnitClass 1/2/8，几个关键等级点）：

| 等级 | Exp1（TBC档，旧） | Exp0（原版档，新） |
|---|---|---|
| 20 | 12.10 | 12.10（一致） |
| 67 | 173.60 | 62.60 |
| 70 | 261.32 | 65.99 |
| 71 | 265.89 | 67.14 |

70级缩放倍数：旧档 261.32/12.10≈**21.6倍**（就是我们一直在手动覆盖的离谱系数）→ 新档 65.99/12.10≈**5.45倍**。且新档在67-71级区间波动很小（5.17-5.55倍），比旧档（14-22倍剧烈波动）稳定得多，佐证"TBC档位"数据本身就是坏的。

**用这19个已修复技能反推验证新公式**：把新公式（Exp0）套到这次会话手动修的全部技能上重算（取等级/原始基数中值估算，仅供量级参考），逐条对照如下：

野外精英/普通怪：

> ⚠️ 下表是 08-13 当天只有19个样本时的初版对照，"新公式算出来"这一列是纯 Exp0（野外场景不叠加副本系数，数值本身仍然准确）。**2026-08-14 全部14条（除38498/12058/20823/20295已提前退役外，其余10条也在同一天追加退役）**，野外这一档已经全部改用公式自动计算，不再有手动覆盖。

| 技能 | 新公式算出来 | 我们定的值 | 对照 | 代码状态 |
|---|---|---|---|---|
| 暗影箭 9613（暗影议会术士等） | ≈388 | 350-600 | ✅ 落在区间内 | ⚙️ 已备注，用公式（差距~22%） |
| 暗影箭 20298（斯克提斯唤魂者） | ≈534 | 500-800 | ✅ 落在区间内 | ⚙️ 已备注，用公式（差距~22%） |
| 火焰投石 38498（愤怒的地灵） | ≈590 | 400-700 | ✅ 落在区间内 | ⚙️ 已备注，用公式（差距~7%） |
| 上古之火 37988（日蚀法师） | ≈658 | 400-700 | ✅ 接近上限 | ⚙️ 已备注，用公式（差距~16%） |
| 邪能火球 36247/37945 | ≈395 | 400-700 | ✅ 接近下限 | ⚙️ 已备注，用公式（差距~39%） |
| 烈焰风暴 16102（伊利达雷大领主，19797分支） | ≈1365 | 1500-2200 | ⚠️ 略低于下限 | ⚙️ 已备注，用公式（差距~36%） |
| 奥术箭 13901（斯克提斯风行者） | ≈338 | 500-800 | ⚠️ 偏低 | ⚙️ 已备注，用公式（差距~92%，全组最大） |
| 水箭 32011（库斯卡海妖） | ≈790 | 400-700 | ⚠️ 偏高 | ⚙️ 已备注，用公式（差距~30%） |
| 闪电链 12058（愤怒的气灵） | ≈545 | 400-700 | ✅ 落在区间内 | ⚙️ 已备注，用公式（差距~1%） |
| 火球术 20823（日怒大法师） | ≈553 | 400-700 | ✅ 落在区间内 | ⚙️ 已备注，用公式（差距~1%） |
| 能量涌动 36534（巴什伊尔奥术师） | ≈832 | 400-700 | ⚠️ 偏高 | ⚙️ 已备注，用公式（差距~34%） |
| 剧毒软泥 40818（德拉诺黑血，每跳） | ≈158 | 150-300 | ✅ 落在区间内 | ⚙️ 已备注，用公式（Object.cpp，差距~42%） |
| 闪电箭 20295（呼啸的裂风者） | ≈529 | 400-700 | ✅ 落在区间内 | ⚙️ 已备注，用公式（差距~4%） |
| 水箭 34425（凶猛的水浪之魂） | ≈322 | 400-700 | ⚠️ 接近下限 | ⚙️ 已备注，用公式（差距~71%） |

**2026-08-14 追加退役说明**：用户看过每条的具体差距（16%~92%不等，"我们定的值"本来就是"400-700"这种粗粒度经验区间，不是精确目标，见"关键发现2"）后，明确要求这10条也全部退役，不再要求跟旧手动值贴近。差距最大的两条（`13901` ~92%、`34425` ~71%）改动后实际数值会有明显变化，特此在表里逐条标出差距幅度存档，方便以后如有实测反馈回查对照。

（`38861`瞄准射击、`38741`玛卡扎顿烈焰之雨没进这张表，仍是 🔧 手动，原因见前文"保留手动覆盖的关键原因"——这两个是结构性原因，不是本次差距大小的问题）

副本精英（蒸汽地窖/奴隶围栏/法力陵墓，有 Schaka 实测数据支撑）：

> ⚠️ 2026-08-14 更新（第二版）：普通/英雄难度分开用不同系数了——原始17个样本统计出的均值本身就是"普通1.71 vs 英雄1.64"（小样本噪声内基本重合，谁高谁低证据不足），但按"英雄应该比普通打得疼"这个符合直觉、而非数据强制要求的设计选择，改成**普通×1.65、英雄×1.75**（用 `map->IsRegularDifficulty()` 判断）。下表新增"难度"列，"新公式算出来"按各自难度对应的系数重算。

| 技能 | 难度 | 新公式算出来 | 我们定的值 | 对照 | 代码状态 |
|---|---|---|---|---|---|
| 冰霜震击 22582 | 普通 | ≈981×1.65≈1619 | 1700-2100 | ⚠️ 偏低~5% | ⚙️ 已备注，用公式（2026-08-14追加退役） |
| 寒冰箭 12675 | 普通 | ≈916×1.65≈1511 | 1290-1760 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 寒冰箭 37930 | 英雄 | ≈1832×1.75≈3206 | 2580-3520 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 毒液箭 37272 | 普通 | ≈818×1.65≈1350 | 1000-1675 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 毒液箭 37862 | 英雄 | ≈1636×1.75≈2863 | 2000-3350 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 闪电箭 15234 | 普通 | ≈916×1.65≈1511 | 1200-1750 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| Arcane Bolt 15254（虚灵航标） | 英雄 | ≈1117×1.75≈1955 | 1675-2225 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 暗影箭 12739（盘牙麻疯者） | 英雄 | ≈818×1.75≈1432 | 1150-1540 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 烈焰冲击 14145（盘牙麻疯者） | 英雄 | ≈1308×1.75≈2289 | 1990-2200 | ⚠️ 超出上限~4% | ⚙️ 已备注，用公式 |
| 冰霜新星 15063（盘牙麻疯者） | 英雄 | ≈327×1.75≈572 | 500-570 | ⚠️ 超出上限~0.4%（几乎持平） | ⚙️ 已备注，用公式 |
| 射击 16100（盘牙麻疯者） | 英雄 | ≈916×1.75≈1603 | 1280-1650 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 暗影箭 15232（血环占卜者） | 英雄 | ≈1246×1.75≈2180 | 1750-2350 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 火球术 17290（地狱火小鬼） | 英雄 | ≈1633×1.75≈2858 | 2250-3050 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 灼烧 36807（血环暗法师） | 英雄 | ≈885×1.75≈1549 | 1600-2150 | ⚠️ 仍偏低~3%，用户要求接受这个偏差直接退役 | ⚙️ 已备注，用公式 |
| 烈焰之雨 36808（血环暗法师，每跳） | 英雄 | ≈929×1.75≈1626 | 1300-1600 | ⚠️ 超出上限~1.6%（几乎持平） | ⚙️ 已备注，用公式（Object.cpp） |
| 暗影箭 15472（影月术士） | 英雄 | ≈1662×1.75≈2908 | 2325-3150 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 腐蚀术 37113（影月术士，每跳） | 英雄 | ≈660×1.75≈1155 | 1000-1180 | ✅ 落在区间内 | ⚙️ 已备注，用公式（Object.cpp） |
| 火球术 14034（地狱火劣魔） | 英雄 | ≈605×1.75≈1059 | 850-1150 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 奥术闪耀 37856（盘牙海妖，直接命中） | 英雄 | ≈867×1.75≈1517 | 1321-1527 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 奥术闪耀 37856（盘牙海妖，每跳） | 英雄 | ≈387×1.75≈677 | 600-680 | ✅ 落在区间内 | ⚙️ 已备注，用公式（Object.cpp） |
| 烈焰风暴 16102（影月召唤者） | 英雄 | ≈1338×1.75≈2342 | 2050-2400 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |
| 火球术 17290（影月召唤者） | 英雄 | ≈1633×1.75≈2858 | 2250-3050 | ✅ 落在区间内 | ⚙️ 已备注，用公式 |

**`22582`/`37865` 追加退役说明（2026-08-14）**：这两个之前因为"`EffectBasePoints1` 是负数，原始基数结构特殊"没敢动。重新查了完整效果结构才发现：那个负数（`-51`）是 Effect1，其实是减速debuff（`MOD_DECREASE_SPEED`），根本不是伤害，一直都不受这条缩放公式影响；真正的伤害在 Effect2（普通基数179、英雄基数359，数据结构完全正常，之前的顾虑是多虑了）。用当前公式重算：普通档≈1619（比1700下限低~5%，同上表所示），英雄档≈3427-3486（落在2900-3500区间内）——两档都在可信范围内，已一并退役（`SpellEffects.cpp` 的 `case 22582: case 37865:` 备注掉，保留代码未删除）。`37865` 本身没有独立算过预测值，跟 `22582` 是同一个 `case` 块，一起处理。

至此，副本精英这张表里也**没有任何手动覆盖**了。

### 33501（暗影新星）追加复核与退役（2026-08-14）

回头核查了 `33501`（暗影新星，暗影迷宫，entry 20656 "Malicious Instructor"）——结构上跟 `22582`/`37865` 不一样：这个技能只有一个效果（`Effect1=2` 直接命中，基数147-170），没有负数基数那种历史遗留问题。但**普通/英雄两个难度分支的公式贴合度差异很大**：

| 难度 | 公式预测 | 我们定的值 | 差距 |
|---|---|---|---|
| 普通 | ≈1400-1550 | 1000-1500 | 偏高 ~12-14% |
| 英雄 | ≈1450-1650 | 2000-2500 | **偏低 ~46-49%**（全部已处理技能中差距最大的一条） |

普通档差距不大，跟其他已退役技能同一水平；英雄档差距接近一半，退役后"恶毒导师"英雄难度暗影新星会从 2000-2500 明显下调到约1450-1650。已把这个差距明确告知用户，**用户在了解差距幅度后，仍选择两个难度都退役**（维持跟之前野外批次一致的处理方式）。

修复：`SpellEffects.cpp` `case 33501` 的 `damage = isRegular ? urand(1000,1500) : urand(2000,2500);` 备注掉（保留代码，未删除），改用公式自动计算。

全局仍保留手动覆盖的只剩：`38861`（瞄准射击，机制特殊）、`38741`（Rain of Fire，玛卡扎顿，开放世界无数据支撑）。

**状态**：✅ 代码已实施，等待编译部署 + 实测反馈。英雄难度这条差距较大，重点关注玩家反馈。

**分难度后的取舍**：普通难度4个样本里，3个（12675/37272/15234）落在区间内，1个（22582）从"1.75统一算法时的✅落在区间内"变回"⚠️偏低~5%"——因为它本来就是普通组里数值最高的一个（原始比值1.937，是这批数据里独立于灼烧之外第二个偏离均值较远的点），用普通组更保守的1.65去套，自然会比用1.75更容易显得偏低，这本身就是"普通/英雄分开算"必然的副作用，不是新问题。英雄难度18个点（含追加退役的灼烧）保持不变，因为它们本来就是按1.75算的。这些手动定的值都已退役成自动公式计算，代码不会再对照这个区间，"偏低/超出"只是留档参考，不影响实际已验证行为。

**结论（08-13 当天的判断，仅19个样本，之后被下面"公式优化方案v2"用34个样本的更完整分析取代，见下）**：
- **野外精英/普通怪**：新公式结果普遍落在或接近手动定的区间。
- **副本精英**：新公式结果只有参考值的一半左右，光靠 Exp0 替换不够，副本场景还需要额外修正——这个发现直接导向了后面的"副本×系数"方案。

**决策（08-13 当天，已被后续工作完全取代，仅保留作决策演变记录）**：暂不合并公式修复，继续手动逐个修；讨论过一个"倍数超过10才回退用原版档位"的替代方案，最终没有采用（连同"公式该不该在Exp0之外再加系数"这个问题，一起被后面"公式优化方案v2"重新解决）。`origin/master` 当时还有86个未审查提交，`v3` 是真分叉不是简单落后，合并需要处理冲突——这一点跟公式方案无关，仍然真实有效。

**状态**：✅ 已被 2026-08-14 的完整实施取代，详见下方"实施记录"及后续几节。

### 公式优化方案 v2（草案，未实施）：基于累计34个已修复技能的数据统计（2026-08-14）

背景：截至目前累计修复了 **34个技能**（19个 + 后续陆续新增的15个），每次都同步记了"新公式（Exp0档）算出来的值"和"我们最终定的值"，数据量已经够统计出规律了，比 08-13 那次只有19个样本时看得更清楚。用脚本把两张表的中值(取区间中点)除以公式预测值，按场景分组统计：

| 分组 | 样本数 | 平均倍数 | 标准差 | 最小/最大 |
|---|---|---|---|---|
| 副本英雄杂兵（直接命中+DoT跳字混合，剔除1个离群值） | 13 | **1.639** | 0.041 | 1.561 / 1.746 |
| 副本普通杂兵 | 4 | 1.712 | 0.131 | 1.610 / 1.937 |
| 副本英雄+普通合并 | 17 | **1.656** | 0.079 | 1.561 / 1.937 |
| 野外非精英怪 | 13 | 1.158 | 0.362 | 0.661 / 1.923 |

**关键发现**：
1. **副本怪（不分普通/英雄难度）的"公式预测值→我们最终定的值"倍数极其集中**，17个样本标准差只有0.079（相对误差~5%），均值 **≈1.65倍**。这个不是巧合——说明公式修复（Exp0）本身方向对了，但对"副本场景"这个维度还是系统性偏低了固定的一截，而且这一截**跟普通/英雄难度无关**（两档均值1.639 vs 1.712，在小样本噪声范围内基本重合），更可能是"副本怪"这个场景本身（不管什么难度）就该比公式算出来的高出~1.6倍。
2. **野外非精英怪没有这种规律**（标准差0.362，是副本组的4倍多），倍数从0.66到1.92都有。但这不代表公式对这一档"不准"——更可能是因为我们这一档定的值本来就是"400-700"这种粗粒度的经验区间（懒得为每个技能单独查资料，反正野外杂兵伤害不算敏感），本身就不是精确目标值，拿来算倍数自然对不上。**这一档暂时没有统计意义上的证据支持要修正公式**。
3. **一个重要的复核发现（2026-08-14 已修复，见下方订正记录）**：H鲜血熔炉"影月术士/影月召唤者"这两个怪当时是按"跟伊利达雷大领主同为Rank=1精英"的逻辑给的值（1500-2200 / 300-500），而不是按"跟同副本地狱火小鬼/劣魔一样是副本杂兵"的逻辑给的。事后用上面的统计口径算，这两个技能的倍数只有0.606-1.113，明显低于副本组的~1.65均值——按"副本"这个维度重新套用后明显偏低，已订正。

**订正**：把"影月术士/影月召唤者"相关的3个技能从"精英rank比对"逻辑改为"副本×1.65"逻辑重新计算：
- **暗影箭（15472，影月术士）**：raw base 254-345，class8/level71，Exp0公式倍数5.548 → 公式预测1409-1914 → ×1.65副本修正 → **urand(2325,3150)**（原 1500-2200）
- **腐蚀术（37113，影月术士，DoT跳字）**：raw base固定119，同倍数 → 公式预测660 → ×1.65 → **irand(1000,1180)**（原 irand(300,500)）
- **烈焰风暴（16102，影月召唤者）**：raw base 227-264，class8/level70，Exp0公式倍数5.452 → 公式预测1238-1439 → ×1.65 → **urand(2050,2400)**（原与伊利达雷大领主共用 1500-2200，现拆分成独立分支；伊利达雷大领主本身是野外精英不在副本里，维持 1500-2200 不变）
- **火球术（17290，影月召唤者）**：raw base 254-345，同 15472，class8/level70倍数5.452 → 公式预测1385-1881 → ×1.65 → **2285-3104**，跟"地狱火小鬼"（同副本，已有 Schaka 真实参考值 2250-3050）几乎完全重合——两者raw base相同、等级相近，直接合并成同一个分支复用 2250-3050，不再单独定值。
无外部参考数据支撑这几个具体数值（跟同副本"地狱火小鬼"火球术那种有 Schaka 实测数据的情况不同），是纯按新发现的"副本×1.65"统计规律推算的，置信度低于有 Schaka 数据支撑的条目，后续如有实测反馈需要重新校准。
修复：`SpellEffects.cpp` 修改 `case 15472`/`case 16102`/`case 17290` 的数值和分支结构；`Object.cpp` 修改 `spellProto->Id==37113` 覆盖的数值。不改动法术ID/数据库，不影响其他共用同一法术ID的怪物。
**状态**：✅ 代码已实施，等待编译部署 + 实测反馈。
4. **一个离群值**：灼烧(36807，血环暗法师)倍数2.119，明显高于副本组其他13个样本。大概率是 Scorch 这个法术本身系数设计跟其他技能不一样（不是公式或分类问题），暂按个案处理，不影响整体结论。

**修复逻辑草案**：在 Exp0 公式修复基础上，叠加"副本场景×1.65"修正（草案版本的完整代码、`IsDungeon()`API核实、`levelGap>=30`门槛等后续补的两处修正，都已经在下面"实施记录"里给出最终版本，这里不重复贴代码）。草案阶段唯一还没被后续记录覆盖到、值得保留的一点：**"野外精英"（Rank=1但不在副本里的怪）当时只有1-2个数据点**（伊利达雷大领主，倍数1.36），样本太少，公式没有对这一档做任何修正，这个数据缺口至今没有被后续工作补上。

**状态**：✅ 已实施（2026-08-14）。用户复核方案后确认"没有遗漏就实施，同时把跟新公式贴近的旧手动修复备注掉"，直接落地，跳过了原计划的"非生产分支先验证"步骤。

### 实施记录（2026-08-14）

**实施前補的一个技术缺口**：原方案里 `IsDungeon()` 的具体语义标了"需要在实现前核实"。查了 `Maps/Map.h` 和 `DBCStructure.h` 才发现 `MapEntry::IsDungeon()` 的实际定义是 `map_type == MAP_INSTANCE || map_type == MAP_RAID`——**它把团队副本也算在内**，如果直接用会把从未验证过的团本也套上×1.65，风险不可控（团本样本数=0）。改用 `map->IsDungeon() && !map->IsRaid()`，严格限定成"5人副本，不分普通/英雄，排除团本和战场"，跟34个样本的真实覆盖范围完全对齐，不做任何超出数据支撑范围的外推。

**⚠️ 上线后自查发现的第二个缺口（用户直接指出）**：第一版实现把 Exp0 替换 + 副本×1.65 直接写在 `if (damage)` 里，而这个分支覆盖的是**所有**带 `SPELL_ATTR_SCALES_WITH_CREATURE_LEVEL` 属性的法术，不只是我们204清单里"spellLevel远低于实际等级"这批真正有bug的——很多正常设计的怪物技能也用这个属性，只是它们的 `spellLevel` 本来就跟怪物实际等级很接近（小等级差，原版设计如此，不受这个bug影响）。之前的写法会把这些**本来就没问题**的技能也强行按新公式重算，副本场景还会被强行乘上 ×1.65，属于扩大了改动范围。**修复**：补回原始204清单排查用的同一个判定标准——`levelGap = 施法者等级 - spellLevel >= 30` 才进入新公式分支，否则原样走没改过的旧公式（施法者自己的 `Expansion`，不叠加副本倍数）。这样改动范围被精确收紧到"确认受影响的这一批"，不多不少。

**核心公式改动**（`Object.cpp`，`WorldObject::CalculateSpellEffectValue`）：
```cpp
if (damage)
{
    CreatureInfo const* cInfo = static_cast<Creature const*>(unitCaster)->GetCreatureInfo();
    int32 levelGap = int32(unitCaster->GetLevel()) - int32(spellProto->spellLevel);
    if (levelGap >= 30)          // 跟204清单排查用的同一个阈值，只处理确认受影响的这批
    {
        CreatureClassLvlStats const* casterCLS = sObjectMgr.GetCreatureClassLvlStats(unitCaster->GetLevel(), cInfo->UnitClass, 0);
        CreatureClassLvlStats const* spellCLS = sObjectMgr.GetCreatureClassLvlStats(spellProto->spellLevel, cInfo->UnitClass, 0);
        if (casterCLS && spellCLS)
        {
            float ratio = casterCLS->BaseDamage / spellCLS->BaseDamage;
            Map const* map = unitCaster->GetMap();
            if (map && map->IsDungeon() && !map->IsRaid())
                ratio *= map->IsRegularDifficulty() ? 1.65f : 1.75f;   // 2026-08-14：普通/英雄分开系数，见下方记录
            value = value * ratio;
        }
    }
    else                          // 不在受影响范围内，原样走旧公式（未改动）
    {
        CreatureClassLvlStats const* casterCLS = sObjectMgr.GetCreatureClassLvlStats(unitCaster->GetLevel(), cInfo->UnitClass, cInfo->Expansion);
        CreatureClassLvlStats const* spellCLS = sObjectMgr.GetCreatureClassLvlStats(spellProto->spellLevel, cInfo->UnitClass, cInfo->Expansion);
        if (casterCLS && spellCLS)
            value = value * (casterCLS->BaseDamage / spellCLS->BaseDamage);
    }
}
```

**旧手动覆盖的去留判定**：对全部34个已修复数据点，用"新公式预测值 / 我们定的值"算贴近度，`±15%` 以内视为"贴近，可退役"（备注掉，不删除，保留代码供以后核对/回滚），否则保留手动覆盖。结果：

**08-14 当天最初的判定结果**（`±15%` 以内视为贴近可退役，否则保留）：18个副本技能ID贴近直接退役，4个野外技能（12058/20823/38498/20295）碰巧贴近也退役，其余全部保留手动覆盖。

**⚠️ 这个"保留"名单当天之后经过3轮追加复核，已经全部清空**——用户逐条问过每条的具体差距幅度后，明确要求即使差距较大（最大的达92%）也一律退役，理由是这些手动值本来也只是粗略经验区间，不是精确目标。三轮追加分别是：
1. `36807`（灼烧）：系数从1.65调到1.75后偏差从~28%收窄到~3%，退役（见"灼烧追加退役"节）。
2. 野外场景剩余10个（`9613`/`20298`/`36247`/`37945`/`13901`/`32011`/`36534`/`34425`/`40818`/`16102`的19797分支）：差距16%~92%不等，用户看过具体数字后要求全部退役（见"野外场景剩余10条手动覆盖追加退役"节）。
3. `22582`/`37865`（冰霜震击）+ `33501`（暗影新星）：这三个原本因为"有独立普通/英雄难度分支"保留，回头复核发现结构本身没问题（`22582`/`37865` 的负数基数其实是无关的减速debuff，不影响伤害计算），差距也在可接受范围（`33501` 英雄档~47%是目前最大的一个），用户同样选择退役（见"33501追加复核与退役"节）。

**最终状态**：全部34个已修复数据点里，**只剩2个仍是手动覆盖**——`38861`（瞄准射击，机制特殊，不在本次统计范围内）、`38741`（Rain of Fire，玛卡扎顿，开放世界无数据支撑）。其余全部改用公式自动计算（备注掉，未删除，代码保留供以后核对/回滚）。

**具体备注掉的32个（`SpellEffects.cpp` 31个 case + `Object.cpp` 4个覆盖，`17290`/`16102` 各含2个entry分支均已退役）**：
`33501`（暗影新星）、`22582`/`37865`（冰霜震击）、`12675`/`37930`（寒冰箭）、`15531`（冰霜新星）、`37272`/`37862`（毒液箭初始命中，SpellEffects.cpp + 对应DoT，Object.cpp）、`15234`/`37664`（闪电箭）、`16102`（烈焰风暴，两个entry分支）、`12058`（闪电链）、`20823`（火球术）、`38498`（火焰投石）、`37988`（上古之火）、`36247`/`37945`（邪能火球）、`13901`（奥术箭）、`9613`（暗影箭）、`36534`（能量涌动）、`40818`（剧毒软泥，Object.cpp）、`34425`（水箭）、`20298`（暗影箭）、`32011`（水箭）、`20295`（闪电箭）、`15254`（Arcane Bolt）、`12739`（暗影箭英雄）、`14145`（烈焰冲击英雄）、`15063`（冰霜新星英雄）、`16100`（射击英雄）、`15232`（暗影箭）、`17290`（火球术，两个entry分支）、`15472`（暗影箭）、`14034`（火球术）、`36808`（烈焰之雨，Object.cpp）、`37113`（腐蚀术，Object.cpp）、`37856`（奥术闪耀，直接命中SpellEffects.cpp + DoT跳字Object.cpp）、`36807`（灼烧）。

**预期的副作用（有意为之，不是bug）**：这32个退役的技能里，有好几个原本"只覆盖了1个已反馈的怪，同一法术ID被其他N个未反馈/未研究的怪共用"（比如 `15531` 冰霜新星被 Tidal Surger 等共用、`12739`/`14145`/`15063`/`16100` 被约10-14个不相关怪共用）。退役后，这些**之前从未被单独处理过的怪物现在会自动套用同一条公式**，如果它们也在副本地图里，会得到合理化的伤害值——这是本次改动范围主动扩大的部分，等于顺带修复了一批未被反馈过的潜在同类bug，需要留意后续是否有这批"新受益"的怪物出现异常反馈（理论上不会比改之前更差，因为改之前它们承受的是原始的21倍级溢出）。

**状态**：✅ 代码已实施，等待编译部署 + 实测反馈。核心公式改动当天先完成了18处退役，后续又经过3轮追加复核退役到32处（见下方几节），最终只剩2处保留手动覆盖，全部记录在案，如实测发现问题可随时把对应的手动覆盖代码取消注释恢复。

### 副本修正系数从 1.65 调到 1.75（2026-08-14，用户要求）

上线后用户问了"这个1.65是自己算的还是哪里来的"——如实说明：**是从17个样本统计出来的均值，不是任何外部文档/官方数据/Nightingale9002提交给的现成数字**，"我们定的值"这个分母大部分来自 `Schaka/TBC-research` 的实测参考，但1.65这个倍数本身是这次会话里自己拟合出来的。用户听完后要求"可以提高一点"，给了几个选项后选择上调到 **1.75**。

**这是主动加的安全余量，不是新数据支撑的调整**：把系数从统计均值1.65往上调，会让分布两端发生变化——原本21个验证点里19个还在区间内，但有3个（`14145`/`15063`/`36808`，都是已退役的旧手动值）会从"落在区间内"变成"轻微超出上限"（4%/0.4%/1.6%，都不大）；作为交换，本来偏低最多的离群值灼烧（`36807`）从偏低~28%收窄到偏低~3%，用户后续要求把这个也一并退役（见下方"灼烧退役"记录）。这几个技能本身都已经退役成自动公式计算，代码不会再去对照这个区间，所以"轻微超出"不影响任何已知已验证的行为——真正的意义在于**给公式覆盖到的其余~200个从未验证过的副本技能多留一点余量，减少后续再出现"伤害偏低"反馈的概率**，代价是这批技能有更小的概率出现"伤害偏高"反馈。两者都还没有真实数据验证，是基于已知分布做的主观取舍。

修复：`Object.cpp` `ratio *= 1.75f;`（原 `1.65f`），仅此一行数值改动，`levelGap>=30` 门槛和 `IsDungeon() && !IsRaid()` 判断逻辑不变。

**状态**：✅ 代码已实施，等待编译部署 + 实测反馈。

### 灼烧（36807）追加退役（2026-08-14）

系数调到1.75后，这个之前被判定为"离群值、保留手动覆盖"的技能偏差从~28%收窄到~3%，用户直接要求退役、改用公式自动计算，不再要求它继续贴合原来的 `1600-2150` 目标区间。

修复：`SpellEffects.cpp` `case 36807` 的 `damage = urand(1600, 2150);` 备注掉（保留代码，未删除），改用公式自动计算。至此，21个副本精英统计点里已有19个退役，当时只剩 `33501`/`22582`/`37865` 三个因"有独立难度分支/原始基数结构特殊"继续保留手动覆盖（⚠️ 这三个后续也在同一天被追加复核退役了，见后面几节，此处保留原始记录）。

**状态**：✅ 代码已实施，等待编译部署 + 实测反馈。

### 副本系数按普通/英雄难度拆开（2026-08-14，用户要求）

用户提出"普通和英雄难度系数可以做下区分"，给了具体数字：**普通用1.65，英雄用1.75**。

**如实说明这个拆分的依据**：最初17个样本统计出来的均值本身是"普通1.71 vs 英雄1.64"（普通4个点、英雄13个点），**普通反而比英雄略高**，只是普通那组样本量太小（n=4），差异在小样本噪声范围内，没法说这个方向是真实规律。这次拆分成"普通1.65、英雄1.75"，是按"英雄难度设计上应该比普通打得更疼"这个游戏设计惯例做的选择，跟原始统计方向（普通更高）正好相反——即用直觉/设计惯例代替了样本量不足的统计结果，如果以后有更多普通难度的数据点，可能需要重新核实这个方向对不对。

修复：`Object.cpp` 用已有的 `map->IsRegularDifficulty()`（代码里 `33501`/`22582` 两处保留的手动覆盖本来就在用这个API区分难度分支）替换掉之前不分难度的固定 `1.75f`：
```cpp
if (map && map->IsDungeon() && !map->IsRaid())
    ratio *= map->IsRegularDifficulty() ? 1.65f : 1.75f;
```
`Changes.md` 副本精英对照表新增"难度"列，普通4个点按1.65重算（`22582` 从"1.75统一算法下的✅落在区间内"变回"⚠️偏低~5%"，因为它本来就是普通组里数值最高、偏离均值最远的一个，这是拆分后必然的副作用），英雄18个点不变（本来就是按1.75算的）。

**状态**：✅ 代码已实施，等待编译部署 + 实测反馈。

### 外部AI复核（2026-08-14）：两个第三方AI对整套公式的评估，及后续核实结果

把整套方案（Exp0替换 + levelGap≥30门槛 + 副本×难度系数）整理成自包含的问题文档，请了两个不同的AI分别评估。两边共同的结论是"整体思路合理，比直接给所有NPC技能伤害除以一个固定倍率的粗暴修法好很多"，但都指出了具体的方法论问题，其中两条可以直接用代码/数据查证，已经核实：

1. **"英雄难度系数比普通高，是不是会跟隐藏的英雄本全局伤害加成叠乘"——查证：不成立**。查了 `Creature.h`/`ObjectMgr.cpp`，mangos-tbc（TBC世代架构）**没有**运行时的"英雄难度伤害倍率"这类全局乘数；TBC的普通/英雄是完全独立的两行 `creature_template` 记录（`HeroicEntry` 只是指针），难度差异已经完全体现在两行各自的等级/职业配置里，没有额外的隐藏叠加层。**风险不存在，不需要改动。**

2. **灼烧（36807）这个离群值，是不是因为它属于设计上不同的技能族群**——查证：**有依据**。查了 `spell_template` 的 `CastingTimeIndex`（读条时间分类）：对照的暗影箭/火球术全部是 `14`（标准3秒读条），灼烧却是 `16`，明显是不同的读条时间分类，跟"灼烧本来就设计成比标准法术读条更快"这个游戏常识吻合——支持"灼烧不是数据/分类错误，是公式适用范围之外的个例"这个判断，继续保留手动覆盖是对的。

3. **"levelGap=29 和 30 之间存在悬崖效应，29的怪完全不修、可能比30的怪更离谱"——查证：理论成立，但实际数据库里没有对应的受害者**。把发现204清单用的 SQL 阈值改成 `BETWEEN 20 AND 29` 重新查了一遍：只有 **11条** 技能/怪物组合（远小于gap≥30的204条），而且**全部是40-47级的经典旧世界内容**（Zanzil/Vilebranch/Atal'ai等），用 `MinLevel>=58` 单独筛了一遍TBC内容，**结果是0条**。更关键的是这11个怪物的 `Expansion` 字段本来就全部是`0`——也就是说它们根本不受Exp1那条坏曲线影响，门槛在不在对它们是等价的。**结论：这个悬崖在当前数据库的实际内容里是空的，不需要现在调整门槛**，但以后版本更新如果引入新内容落在这个区间，需要重新核查。

4. **两边都指出的方法论问题，如实记录，不辩解**：
   - 用来拟合1.65这个系数的样本点，同时也被当成"验证公式对不对"的点，存在循环论证——目前没有独立于拟合数据之外的验证集。
   - "1.65±0.04"这类置信区间描述的是**均值本身的不确定性**，不能直接理解成"剩下没验证过的~180个技能大概率也落在这个区间"——对全新技能的预测不确定性明显更大，不应该拿置信区间当预测区间用。
   - **`1.75` 这个数字应该明确标注成"主观加的工程安全余量（policy）"，不是"统计估计值（statistical estimate）"**——虽然两个1.65-1.71之间的置信区间/容忍区间计算能说明1.75不算离谱，但它终究是我们在16-24小时内一次对话里拍板决定的，不是有独立数据支撑的结论。上面"副本修正系数从1.65调到1.75"和"普通/英雄难度拆分"两节的表述已经是如实写的（明确写了"是主动加的""不是新数据支撑的""跟统计方向相反"），这里再次强调，避免以后回看这份文档时把1.75/1.75误当成有统计把握的数字。

**后续建议方向（两边AI共同提到、本次未实施，留作以后参考）**：把系数做成配置文件参数而不是硬编码常量，方便后续不用重新编译就能调整；持续积累新的实测反馈作为独立验证集，而不是继续在现有这批已用于拟合的数据上争论系数细节。

**状态**：ℹ️ 复核记录，不涉及代码改动（两条查证结果确认现有实现不需要调整），仅补充文档措辞准确性。

### 野外场景剩余10条手动覆盖追加退役（2026-08-14，用户要求）

外部AI复核之后，用户回头问了"野外场景/普通怪对照表"里剩下的手动覆盖具体差距多大——列出每条差距（16%~92%不等）后，用户明确要求"这些都全部备注，套用公式"，不再要求它们贴合原来手动定的"400-700"经验区间。

**处理的10个法术ID**（对照表见前面"野外精英/普通怪"表，每行都标了具体差距）：`9613`/`20298`/`37988`/`36247`/`37945`/`13901`/`32011`/`36534`/`34425`（`SpellEffects.cpp`）、`40818`（`Object.cpp`）、以及 `16102` 的 `19797`分支（伊利达雷大领主，之前唯一保留的分支）。

修复：以上10个法术ID的 `damage = urand(...)`/`value = irand(...)` 全部备注掉（保留代码，未删除），改用公式自动计算。至此，**野外场景这一档已经没有任何手动覆盖**，只剩 `38861`（瞄准射击）、`38741`（Rain of Fire）两个因结构性原因（不是差距大小）继续保留。

**提醒**：这批里差距最大的两条（`13901` ~92%、`34425` ~71%）改动后实际数值会有明显变化，不是"轻微超出"级别的调整，上线后需要重点关注这两个技能有没有新的玩家反馈。

**状态**：✅ 代码已实施，等待编译部署 + 实测反馈。

---

## upstream（官方 cmangos/mangos-tbc）逐个提交审查

`v3` 分支已加 `upstream` 远程（`https://github.com/cmangos/mangos-tbc.git`），当前落后 upstream/master 69 个提交（领先 20 个本地/Nmangos-tbc 自己的改动）。逐个从最早的开始审查，一条一条处理，分析完先记录在这里，确认后再实施（合并进仓库文件 + 同步到 `tbcmangosdev`）。

**当前进度**：已审查到第 24 条（第24条改动太多，用户决定暂不合并），下次从第 25 条继续。

| # | 提交 | 摘要 | 影响 | 状态 |
|---|---|---|---|---|
| 1 | `ed731ae4d` | 塞斯克鱼人湾巨蜥"毒液箭雨"(38655) `MaxAffectedTargets` 0→10 | SSC 单个精英怪AoE命中人数上限，低风险 | ✅ 已 cherry-pick（`f2f90e184`），`tbcmangosdev`/`tbcmangos2` 已执行等效 UPDATE |
| 2 | `15c7a3ee9` | 玄冰石阶通关后逃生传送门重进不可交互 | 5人本QoL修复，低风险 | ✅ 已 cherry-pick（`463c976b2`），待编译部署 |
| 3 | `5c9580a64` | 逃生传送门法术(46841)标记为 TRIGGERED（配套#2，抓包比对） | 纯触发方式修正，低风险 | ✅ 已实施 |
| 4 | `ff681a044` | `CheckTarget` 对纯 `TARGET_FLAG_UNIT` 目标跳过友敌校验 | 核心引擎级改动，作者自述半成品（ALLY/ENEMY标记未处理），影响面广 | ⏭️ 跳过，待更多证据 |
| 5 | `ea90dbcb1` | 斯坦索姆 Maleki the Pallid"冰封墓穴"命中清空目标仇恨 | 单一5人本BOSS机制，低风险 | ✅ 已 cherry-pick（`4ff66b28d`），待编译部署 |
| 6 | `2d9e0e24a` | 地狱火脓水沼泽 Hungarfen 孢子云DoT误伤自己（可能被bug秒杀）+缩小技能漏施放 | 单一5人本BOSS核心机制修复，价值较高；需代码+`spell_scripts`绑定两步 | ⏸️ 待确认，尚未实施 |
| 7 | `aa39545ce` | 怪物"说话"广播范围按可见距离(AOI)比例放大 | 纯客户端体验层，无副作用，低风险 | ✅ 已 cherry-pick（`7279de930`），待编译部署 |
| 8 | `10b579ceb` | 补充缺失的 spell_template 数据行：32432 "Full Heal / Mana"（纯数值，无对应 Effect 类型异常） | 纯附加 INSERT，但当前 Nmangos-tbc 代码/DB 均无处引用该 ID，没有实际用途 | ⏭️ 用户判断没必要实施，已撤销（revert 提交 `e933a8b47`，`tbcmangosdev` 已删除对应行并核实） |
| 9 | `55782dc5b` | 邮件到达提醒弹窗最多显示 2 封→3 封 | 纯客户端展示，低风险 | ✅ 已 cherry-pick（`b7a3ef146`） |
| 10 | `b8e2c77ac` | `ITEM_DYNFLAG_UNK1` 重命名为 `ITEM_DYNFLAG_TRANSLATED`+注释；`SMSG_READ_ITEM_FAILED` 补两行注释（未激活代码） | 纯命名/注释澄清，零功能变化 | ✅ 已 cherry-pick（`56dfc52e9`） |
| 11 | `d01165a0c` | `SpellTargetMgr` 目标消费判定补两处 `TARGET_TYPE_NONE` 分支（针对 spell 43178/51957 实测问题） | 核心目标校验，改动小且有具体证据支撑，但波及全体法术的共用逻辑，具体影响面未逐一排查 | ⏸️ 用户决定先不合并、观察，已 revert（`40ae7c51d`） |
| 12 | `e61ca75bb` | PvP 击杀荣誉的"对方段位加成"取值改为读 `LIFETIME_MAX_PVP_RANK` 字段而非当前称号 | **本仓库这个字段从未被任何代码写入过（恒为0），且同区域 `CONDITION_PVP_RANK` 也是硬编码 `return false`——这套经典PvP段位系统在本库本来就是占位/未实现状态**。合并后段位加成会变成恒定按0计算 | ⏭️ 用户决定跳过不合并，保留现有基于称号的旧逻辑 |
| 13 | `566ea5b87` | AV 战场"地图完成"荣誉奖励漏调用 `GetBonusHonorFromKill()` 转换（同文件其他所有奖励调用点都有转换，唯独这处没有，5年老bug） | 单一战场，荣誉数值偏低，同文件交叉核对确认 | ✅ 已 cherry-pick（`4eb1ac45d`），待编译部署 |
| 14 | `fb94568ea` | `SMSG_SUMMON_REQUEST` 发送 `GetZoneId()`（区域）改为 `GetAreaId()`（子区域），修正传送确认框显示的召唤者位置 | 纯客户端显示，低风险 | ✅ 已 cherry-pick（`f7df5905b`） |
| 15 | `7bf1caee6` | `SMSG_SPELL_UPDATE_CHAIN_TARGETS`（引导技能连锁目标更新包）按抓包新证据调整结构 | 客户端视觉表现（引导法术连锁目标线），低风险 | ✅ 已 cherry-pick（`e957f270d`） |
| 16 | `a8a5701da` | 同上，第二次按更多抓包证据再调整（#15 的后续修正） | 同上 | ✅ 已 cherry-pick（`cc8f79176`），待编译部署 |
| 17 | `83135c4cb` | 新增 `Player::SendOpenContainer`，装入背包槽后自动发送 `SMSG_OPEN_CONTAINER` 弹出容器窗口 | 纯客户端 QoL 新功能，自包含，低风险 | ✅ 已 cherry-pick（`63c45a9d5`） |
| 18 | `9873b5955` | `CMSG_BUY_BANK_SLOT` 补上 slot 上限校验，防止客户端传入越界值 | 输入合法性加固，低风险 | ✅ 已 cherry-pick（`05e2a3dbd`） |
| 19 | `1209c1f93` | 修复 `SpellCastArgs` 未初始化成员 `m_itemSet`/`m_itemTarget` | 跟本仓库 2026-08-04 独立修复的 Mana Tombs 崩溃是**同一个 bug、同一处修复**，本地代码已完全一致 | ✅ 已通过本地独立修复覆盖，无需重复 cherry-pick |

| 20 | `9f4060f3c` | MinGW 编译兼容性调整（`_MSC_VER`→`_WIN32`、winmm链接位置） | 对现用的 MSVC 编译零行为差异，纯粹是给 MinGW 工具链铺路 | ✅ 已 cherry-pick（`fe48562e9`） |
| 21 | `022a8f141` | 法术14108"剧毒蛇皮毒药" `Dispel` 4→0，修正施法者对毒免疫时连自己都被误挡、放不出这个效果的问题 | `tbcmangosdev` 当前该字段已经是0，此次合并对现有数据零实际影响，只是让基础建库文件保持与upstream一致 | ✅ 已 cherry-pick（`2101d99e6`），无需对DB做任何操作 |
| 22 | `b8b83dcf4` | CppCheck 静态分析扫出的6处小问题：5处是等价写法清理（无符号数判断、冗余分支等），1处是真实小bug（`SpellCastTargets`拷贝赋值漏拷贝`m_CorpseTarget`字段） | 前5处零行为变化；第6处影响面窄（仅目标信息被复制且涉及尸体目标的法术），但是真实修复 | ✅ 已 cherry-pick（`b172b4a67`） |
| 23 | `f8926ff41` | 反作弊模块 `antispam.cpp` 两处可变参数函数补齐缺失的 `va_end`，修正未定义行为 | 只影响反作弊内部GM通知日志函数，不涉及游戏逻辑 | ✅ 已 cherry-pick（`0ab5ef6e9`） |
| 24 | `314069eeb` | CppCheck "缺失override" 批量清理，涉及32个文件：补virtual/override关键字、删空虚析构、删子类里跟父类重复的成员声明 | 改动文件数多；每类改动本身是编译器强制校验或纯清理，概念风险不高，但体量大 | ⏸️ 用户觉得改动太多，暂不合并 |

**待办**：#6 需要用户确认；#12 已跳过；#24 暂不合并；upstream 这条线到此为止不再继续——`origin/master`（Nightingale9002 fork）后续被发现前24个提交跟这张表完全是同一批，第25条起转移到下方"origin/master 逐个提交审查"继续。

## origin/master（Nightingale9002/Nmangos-tbc）逐个提交审查

`v3` 落后 `origin/master` 87 个提交、领先52个。前24个提交跟上面"upstream"表是完全相同的commit（Nightingale9002 fork 本身定期同步 cmangos 官方），直接沿用上表已有的决定，不重复审查。**从第25条开始**逐条审查，跟 upstream 审查一样一条一条来，确认后再合并。

| # | 提交 | 摘要 | 影响 | 状态 |
|---|---|---|---|---|
| 25 | `dbaa657da` | 补齐13个文件、77处"字段偏移量+枚举值"运算的显式 `static_cast`，消除编译警告（#634） | 抽查多个改动量最大的文件，全部数值等价的显式转型，无实际运算逻辑改变；跟已合并的 #22 同一类改动，只是规模更大 | ✅ 已 cherry-pick（`6bb166cef`） |
| 26 | `df76f6908` | 艾拉隘口战场 `BattleGroundAV.cpp` 一行：摧毁塔楼奖励声望的三元表达式两个分支都写成暴风大军（复制粘贴错误），修正部落分支为冬狼氏族 | 单行改动，逻辑清晰，真实游戏bug（部落摧毁塔楼被错误奖励联盟阵营声望），只影响AV声望奖励 | ✅ 已 cherry-pick（`91f696a7a`） |
| 27 | `0f557959c` | `WorldSession::Update()` 会话复用分支绕过了排队检查，且漏发 `SendClientCacheVersion`/`SendTutorialsData` 两个握手包，导致客户端在会话复用场景下崩溃；统一两个分支的排队逻辑并补发遗漏的包 | 触碰核心登录/会话流程，影响面是全部玩家，但改动本身收窄清晰（统一分支+补发遗漏包，非新增逻辑）；`SendTutorialsData` 幂等，重复调用无副作用 | ✅ 已 cherry-pick（`885133550`） |
| 28 | `28ef97c3a` | 猎人天赋"精良宠物治疗"(19572/19573)从旧的硬编码 `case 4086/4087` 迁移到新的 `spell_scripts` 脚本系统，逻辑等价，但需要配套SQL（`spell_scripts` 新增2行绑定），且代码侧已删除旧逻辑，SQL不跑天赋就会失效 | 涉及DB且新旧逻辑非兼容并存（必须代码+SQL同时生效） | ⏸️ 用户选择暂不合并 |
| 29 | `d48be200e` | 卡拉赞策展者(Curator)"星界耀斑"召唤物追击玩家的仇恨值，`100001.f` → `1000.f` | 单行改动，改动范围极窄（只影响这一个boss召唤物的仇恨机制），具体原始bug表现未完全查证到根源，但爆炸半径小 | ✅ 已 cherry-pick（`b06b4d4f2`） |
| 30 | `43162a9da` | `Spell::EffectPowerBurn` 删除一行人为拼凑的战斗日志（原注释自述"客户端没实现POWER_BURN显示"的workaround），作者基于抓包证据确认真实协议不发这条日志 | 纯删除，不影响实际伤害/效果，只影响法力灼烧类法术的战斗记录显示细节 | ✅ 已 cherry-pick（`1f9c81b0d`） |
| 31 | `c55d84514` | 黑色圣殿"遗物之魂"boss战"灵魂尖啸"(41545)命中后清空目标怒气，作者自述是"hack"（伤害结算与怒气生成顺序问题的临时补丁），需要配套SQL新增 `spell_scripts` 绑定 | 范围极窄（单boss单技能），作者自认是workaround非通用修复 | ⏸️ 用户选择暂不合并 |
| 32 | `3098491fd` | 新增 `SendMessageToAllWhoSeeMeMove` 替换7处移动广播调用点，修正被魅惑/操控生物移动时会把移动包"回声"给操控者自己客户端（旧函数的 `self` 参数被基类忽略），造成画面抖动/回弹 | 核心移动广播路径，但新增辅助函数+替换调用，旧函数不删除不影响其他调用方；只在魅惑/操控场景有实际行为差异 | ✅ 已 cherry-pick（`1076c2cd8`） |
| 33 | `ad18b2013` | 黑色圣殿泰隆·尸风(Teron Gorefiend)boss战3个法术脚本结构体补充法术名/ID注释 | 纯注释，零逻辑改动 | ✅ 已 cherry-pick（`500627de5`） |

| 34 | `264805cef` | 海加尔山刷波逻辑：`SetVariable(...,enemyCount-1)` 没有同步递减本地变量，导致本波最后一个敌人死亡时"是否清空/该刷下一波"判断误判 | 单行改动，逻辑bug清晰，只影响这一个团队本的刷波机制 | ✅ 已 cherry-pick（`b03bca3ed`） |

| 35 | `2a623a99b` | 黑色圣殿伊利丹议会 Gathios "审判"(41467)新增法术脚本，读取当前圣印(命令/鲜血)光环存的法术ID并对目标施放，还原玩家职业"审判消耗圣印"机制 | 单boss单技能新增，不影响其他内容；需配套SQL（已执行） | ✅ 已 cherry-pick（`d612b6c2a`），`tbcmangosdev`+`tbcmangos2` 均已执行 `INSERT INTO spell_scripts` 并核实 |

| 36 | `e475b84df` | 给 `JudgementGathios` 补上"消耗后移除对应圣印光环"逻辑（读取圣印效果值时一并 `RemoveSpellAuraHolder`），还原"审判消耗圣印"机制 | 单boss单技能，基础脚本(#35)已合并，本条正常应用无冲突 | ✅ 已 cherry-pick（`0653f8639`） |

| 37 | `45fd2be9d` | 海加尔山Anetheron召唤地狱火：进战斗时机从"延迟1.5秒"提前到"召唤瞬间"并锁定AI战斗脚本状态，避免中途脱战判定误删"燃烧"光环；配套DBC给燃烧光环(31304)加脱战不清除属性 | 单boss机制修复，范围窄；DB为 `AttributesServerSide` 位运算UPDATE，非破坏性 | ✅ 已 cherry-pick（`c79ed4921`），`tbcmangosdev` 已执行UPDATE并核实，`tbcmangos2` 待确认 |

| — | `c45e4c8a1` | `Unit.cpp` 唯一还在用裸数字0/1索引"攻击强度修正正负号"数组的地方，换成已存在的同名枚举 `AttackPowerModSign::MOD_SIGN_POS/NEG`，数值完全一致 | 纯可读性统一，零行为改变 | ✅ 已 cherry-pick（`3d42d10c7`） |

**⚠️ 编号方式订正**：手动数序号（`git log \| head -N` + 肉眼扫描）不可靠，之前已经因此漏看/错标过（`e475b84df` 那次冲突）。改用 `git log --reverse --oneline --cherry-pick --right-only v3...origin/master` 作为权威"剩余未处理"清单（自动排除已经以任意方式合并/等效处理的提交，不依赖手动计数），后续不再用 `#序号` 标注，直接按 commit hash 记录。

| — | `83a047a9c` | 修复回归bug(`75b1d0a8b`引入)：`UpdateAttackPowerAndDamage` 里"攻击强度按属性百分比加成"光环的计算结果只存到本地变量，写入实际生效字段时用的是另一个成员数组来源，两者脱节导致萨满"精神敏锐"、猎人"精确瞄准"、战士"武装到牙齿"三个天赋失效；补上两者差值再写入 | 触碰全角色高频路径（攻击强度计算），但改动收窄——不受影响的角色差值恒为0，只有这三个天赋的使用者有实际差异 | ✅ 已 cherry-pick（`4a3bc13c0`） |

| — | `9ca61008c` | 上一条(`83a047a9c`)的直接后续微调：去掉"算总和再减回去"的多余计算，改成直接累加`statBonus`；取整顺序从"两数分别转int32再相加"改成"先按float相加、最后一次转int32"，对齐官方服务端全程float精度运算的行为，减少截断误差 | 同一函数的紧邻改进，逻辑等价+精度对齐 | ✅ 已 cherry-pick（`9ef61643f`） |

| — | `56541087d` | `AddCooldown()` 对冷却时间(uint32)套用技能改动时，减CD效果数值超过当前冷却本身会无符号下溢，变成~40亿(表现为12小时离谱CD如"闭嘴")；改用有符号中间值+clamp到0修复。顺带把 `recTime`/`categoryRecTime` 的技能改动应用从`else if`（互斥）改成两个独立`if`（原代码分类CD在有独立CD时改动永远不生效）；另加已过期冷却存档时跳过的小优化 | 全技能通用高频路径，下溢修复是明确真实bug；"两个if都生效"是行为变化，commit未明确提及，未经额外验证 | ✅ 已 cherry-pick（`c0fe73b4e`） |

| — | `87ee9b377` | 把原来硬写在包处理函数里的"椅子距离检查"抽成正式的 `GameObject::CanUseNow(Player const*)` 方法，新增"召唤仪式"类物件校验（战斗中/非同队不可用）；顺带修了椅子距离检查漏传运输工具参数、载具上椅子距离可能算错的问题 | switch里只特殊处理椅子和召唤仪式两类GO，其他类型`return true`默认放行、行为不变 | ✅ 已 cherry-pick（`f1171d80e`） |

| — | `29f7fc0d3` | 继续把GO使用检查搬进 `CanUseNow` 并新增多条：载具/变身状态限制、宝箱战斗中不可用、锁定物件从"无条件拒绝"改成"没有对应开锁技能才拒绝"（实质行为变化，有开锁技能的职业可直接用use包开锁）、开锁技能需求为0时按等级×5兜底 | 影响面比上一条大，锁定物件那条是玩法行为变化；变身检查作者自留注释`meant to have an can unshift check here`自认不完整 | ✅ 已 cherry-pick（`f97386d72`），用户已知悉上述隐患仍选择合并 |

| — | `9e15e216f` | 补前一条(`29f7fc0d3`)的坑：载具检查从泛化的`!IsSelfMover()`收紧为"骑乘+GO不支持骑乘可用+(飞行任务中或客户端不可控)"，普通骑马正常操作不再被误挡；变身检查补充"客户端不可控"和"该变形带不自动脱离标记"两种情况，`meant to have an can unshift check here` 注释仍保留但逻辑更贴近实际场景 | 同一函数内部微调，方向正确、范围未扩大 | ✅ 已 cherry-pick（`399214911`） |

| — | `5af5ea8dc` | `ScriptMgr.cpp` "按半径搜索DB脚本目标"逻辑，range-for遍历vector的同时erase元素（迭代器失效，UB），改成标准remove-erase惯用法 | 纯正确性修复，过滤条件逻辑不变，只是删除时机安全化；影响面是所有用`searchRadiusOrGuid`的DB脚本 | ✅ 已 cherry-pick（`a9e351498`） |

| — | `3c8121d3d` | 修正 `29f7fc0d3`（我们已合并）里"战斗中开箱"检查极性写反的bug：`IsUsableInCombat()`返回true=允许战斗中用，原判断写成"允许用且在战斗中→拒绝"正好相反，补`!`修正 | 单字符修复，逻辑清楚，直接修正我们自己刚合并的bug | ✅ 已 cherry-pick（`171886c45`） |

| — | `9ea7c43ef` | `MoveSplineInitArgs::Validate()` 的 `CHECK` 宏验证失败时记日志直接解引用 `unit`，但某些路径（"循环样条延续"）unit可以是空指针，导致记录错误日志本身崩溃；加判空处理 | 纯防御性判空，只在已出错路径生效，不影响正常流程；关联真实上游issue | ✅ 已 cherry-pick（`394d9a4b9`） |

| — | `3fa107236` | 给对话辅助结构体新增 `type`（ACTION/TEXT，默认ACTION）字段，"直接指定说话者+文本"分支改成要求`type==DIALOGUE_STEP_TEXT`才触发；查了origin/master全部历史，`DIALOGUE_STEP_TEXT`没有任何脚本文件实际设置过，等于静默关闭这个现有机制，看起来是没配套完成数据标注的半成品重构 | 单独合并会静默禁用全部依赖此分支的现有boss对话/喊话脚本 | ⏸️ 用户选择暂不合并，等后续配套提交出现再处理 |

| — | `a3f4bc370` | `boost/bind.hpp` 换成新的 `boost/bind/bind.hpp`，配合`using namespace boost::placeholders;`，消除Boost弃用警告 | 纯编译期头文件调整，不影响运行时行为 | ✅ 已 cherry-pick（`44fc3db10`） |

| — | `1953d2fa1` | 把`SpellVisual`数组比较从直接`==`（比较指针，永远false）改成`std::equal`逐元素比较 | 查证我们代码库及origin/master整个历史`SpellVisual`始终是标量`uint32`不是数组，这条改动大概率是从WotLK分支照搬未做TBC适配，`std::begin/end`对标量调用编译不过；且TBC下原始`==`比较本身没有bug | ⏭️ 用户选择不合并，编译大概率失败且无实际bug需要修 |

| — | `5d9039384` | 早前已合并的`aa39545ce`（说话范围按可见距离放大）延伸：那次只覆盖了常规AI喊话函数，`DoScriptText`（DB脚本驱动喊话）走独立函数没覆盖到，这次补齐 | 与已接受逻辑一致，纯客户端体验层 | ✅ 已 cherry-pick（`dc8b1c2fc`） |

| — | `0b83de892` | `[s2491]` 纯改名迁移：`quest_template.ReputationSpilloverMask` → `RewFactionFlags`，配套C++变量/getter同步改名，跟WotLK命名对齐，位掩码语义不变 | 纯改名，零行为改变 | ✅ 已 cherry-pick（`94c492a08`），`tbcmangosdev` 已执行迁移SQL并核实，`tbcmangos2` 待确认 |

**⏸️ 本轮逐条审查到此暂停（2026-08-14），已处理到 `0b83de892` 为止。**

### ⚠️ 编译修复：`0f557959c` 引入的符号未定义错误（2026-08-14，用户实际编译时发现）

用户在Linux实际编译时报错：`WorldSession.cpp` 的 `SendClientCacheVersion()` 用到的 `SMSG_CLIENTCACHE_VERSION`/`CONFIG_UINT32_CLIENTCACHE_VERSION` 两个符号在本仓库**完全没有定义**（全仓库搜索确认，不是漏包含头文件）。这正好印证了之前分析 `ff9de1d6e`（"Fix port compilation"）时的判断——那条不是"port特有"问题，是这两个符号本身在TBC这份代码里就不存在（大概率 `0f557959c` 是照搬/误用了WotLK才有的客户端缓存版本协议）。

**之前的复核疏漏**：本轮23个 `origin/master` cherry-pick 全程只做了大括号配对等静态文本检查，**没有实际编译验证**，这个符号未定义错误没能提前发现，是靠用户自己编译时才暴露的。

**修复**：不是整体合并 `ff9de1d6e`（那条会连带撤销 `0f557959c` 其他仍然有价值的部分），而是精确只移除 `SendClientCacheVersion()` 方法本体（`.cpp`+`.h`声明）和它在 `Update()` 里的唯一调用点，`0f557959c` 的其余部分（排队分支统一判断、`SendTutorialsData()` 补发）原样保留。顺带把 `ff9de1d6e` 里另一处看起来无害且用户build环境（Linux/GCC）大概率同样需要的 `#include <boost/bind/placeholders.hpp>`（`AsyncListener.hpp`）一并加上，防止下一次编译在这里再卡一次。

修复文件：`src/game/Server/WorldSession.cpp`、`src/game/Server/WorldSession.h`、`src/shared/Network/AsyncListener.hpp`。

**状态**：✅ 代码已改，等待用户重新编译验证。仍建议全量重新编译一遍，确认没有其他未发现的类似问题（本轮cherry-pick没有做过实际编译验证，不能保证这是唯一一处）。

**待办**：下次继续时先处理 `ff9de1d6e` 的决定，再用 `git log --reverse --oneline --cherry-pick --right-only v3...origin/master` 查出后续，继续逐条审查。
